# 技能索引 · mfm-admin

与本仓库 **实际存在的** `.agents/skills/<name>/SKILL.md` 对齐。任务前按场景打开对应 `SKILL.md`。

**维护**：新增或删除技能目录后，同步更新本表，并执行 `pnpm verify:harness`。

**跨项目通用技能**（Grill Me、PRD、TDD、Harness 工程等）与 **mfm-uniapp-vue3** / **mfm-api** 共用同名目录；需要时可从兄弟仓库复制到本仓库 `.agents/skills/` 并在下表登记。

---

## 本项目

| 技能         | 场景                                     | 路径                              |
| ------------ | ---------------------------------------- | --------------------------------- |
| 运营后台开发 | pure-admin + `/admin/*`、菜单、集合 CRUD | [mfm-admin](./mfm-admin/SKILL.md) |

---

共 **1** 项技能（本项目）。扩展通用技能后记得更新本表并跑 `pnpm verify:harness`。

## 文档入口

- Harness 入口：`AGENTS.md`
- HTML 门户：`doc/index.html`
- Admin API 路由表：`doc/admin-api.html`
